# config file for plotting data from XYA file
# for each variable need to list
# format for line starting with data:
#           data; 'var_name';num_id;pressure_levels(comma separated)
#               pressure_level MUST be 'none' for 2d variables
#           example: data,'temp_raw',1,200
#           num_id = 1 - plot_lon_lat_data
#
#format for variables line
#time;variable_to_plot (2d or 3d if pressure level
#provided);lons;lats;storm_munber;storm_basin;keywords(; separated)

# --- temperature from XYA file  -  - contour plot on the map
data;T MetopB;1;250
swath_time;xya_t;slon;slat;asnum;abasin;add_lon = 0;add_lat = 0;storm_name = storm_name;units = K;savefig = True;show = False;plt_type = scatter;

# --- UV-barbs, Z - contour from XYA file  - contour plot on the map
#data;UVZ AMSU;1;200,250,300,400,500,600,700,850
data;UVZ MetopB;1;850
swath_time;xya_z;slon;slat;asnum;abasin;add_lon = 0;add_lat = 0;storm_name = storm_name;units = m;savefig = True;show = False;plot_var2 = xya_u;plot_var3 = xya_v;plt_type = barbs;barbs_subplot_type = contour;

